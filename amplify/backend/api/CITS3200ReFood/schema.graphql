type FOODITEM @model @auth(rules: [{allow: public}]) {
  id: ID!
  title: String
  pickup_date: AWSDate
  category: String
  transport_reqs: String
  picture: AWSURL
  donorID: String
  nfpID: String
  pickup_location: String
  quantity: String
}
 